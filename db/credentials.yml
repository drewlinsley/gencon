postgresql_credentials:
    username: aizenman_vta
    password: aizenman_vta
    database: aizenman_vta
machine_credentials:
    username: None
    password: None
    ssh_address: None
postgresql_connection:
    database: "${postgresql_credentials.username}"
    user: "${postgresql_credentials.user}"
    password: "${postgresql_credentials.password}"
    host: "localhost"
    port: ""

